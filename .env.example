# Local development values only. Never commit production credentials; inject them
# through the approved secret manager.
DATABASE_URL=postgresql://claim_solution:claim_solution_local@localhost:5432/claim_solution?schema=public
NEXT_PUBLIC_API_BASE_URL=http://localhost:3001/api/v1
APP_PORT=3001
